const MOV_LIT_R1 = {
    value: 0x10,
    label: 'Move literal into r1 register'
};
const MOV_LIT_R2 = {
    value: 0x11,
    label: 'Move literal into r2 register'
};
const ADD_REG_REG = {
    value: 0x12,
    label: 'Add register1 to register2'
}

module.exports = {
    MOV_LIT_R1,
    MOV_LIT_R2,
    ADD_REG_REG
}